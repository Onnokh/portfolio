# Rybbit, the visitor count (see src/rybbit.ts). Not a secret: the ID is in every page it counts.
# Vite inlines it at build time, so the Docker build reads it from here.
VITE_RYBBIT_SITE_ID=290c46c7c834
